services:
  - type: web
    name: mitralink-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        value: mitralink_secret_2026
      - key: GROQ_API_KEY
        fromContext: git
      - key: GEMINI_API_KEY
        fromContext: git
      - key: DATABASE_PATH
        value: /app/data/database.sqlite
    disk:
      name: sqlite-data
      mountPath: /app/data
      sizeGB: 1
